<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Product — Supermarket App</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    :root{
      --accent:#0d6efd;
      --muted:#6c757d;
      --card-radius:12px;
    }
    html,body{height:100%;}
    body {
      margin:0;
      background: linear-gradient(180deg, rgba(224,242,255,0.55), rgba(218,237,255,0.4));
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      color:#0b2540;
    }

    /* Navbar */
    .top-nav {
      background: linear-gradient(90deg, rgba(11,35,64,0.98), #0b233f);
      box-shadow: 0 6px 20px rgba(11,35,64,0.12);
    }
    .top-nav .navbar-brand {
      background:#fff; color:#0b2540; padding:.35rem .75rem; border-radius:8px; font-weight:700;
      display:flex; gap:.6rem; align-items:center;
    }
    .top-nav .nav-link { color: rgba(74,158,255,0.95) !important; }
    .top-nav .nav-link:hover { color:#fff !important; text-decoration:underline; }

    /* Centered card */
    .page-wrapper { max-width:720px; margin:2.25rem auto; padding:0 1rem; }
    .edit-card {
      background: linear-gradient(180deg,#ffffff,#fbfdff);
      border-radius: var(--card-radius);
      padding:1.25rem;
      box-shadow: 0 12px 36px rgba(16,24,40,0.06);
      border:1px solid rgba(13,110,253,0.04);
    }

    .card-title { font-weight:800; font-size:1.25rem; margin-bottom:.25rem; }
    .card-sub { color:var(--muted); margin-bottom:1rem; }

    .form-group { margin-bottom: .85rem; }
    label.form-label { font-weight:600; color:#243447; }

    .image-preview {
      width:160px; height:120px; border-radius:8px; overflow:hidden;
      background:#f6fbff; display:flex; align-items:center; justify-content:center;
      border:1px solid rgba(13,110,253,0.06);
    }
    .image-preview img { width:100%; height:100%; object-fit:cover; display:block; }

    .btn-update {
      border-radius:10px;
      padding:.6rem 1rem;
      font-weight:700;
    }

    .back-link { color:var(--muted); text-decoration:none; }
    .back-link:hover { text-decoration:underline; color:#000; }

    @media(max-width:575.98px){
      .image-preview { width:120px; height:96px; margin:auto; }
    }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-sm top-nav">
    <div class="container page-wrapper">
      <a class="navbar-brand" href="/">
        <span style="width:34px;height:34px;border-radius:6px;display:inline-flex;align-items:center;justify-content:center;background:linear-gradient(180deg,#e9f5ff,#dff0ff);color:var(--accent);font-weight:800">🛒</span>
        <span>Supermarket App</span>
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/addProduct" style="color: #4a9eff;">Add new product</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="page-wrapper">
    <div class="edit-card">
      <div class="d-flex justify-content-between align-items-start mb-3">
        <div>
          <div class="card-title">Update Product</div>
          <div class="card-sub">Edit product details and upload a new image (if needed).</div>
        </div>
        <div class="text-end d-none d-sm-block">
          <a href="/" class="back-link"><i class="bi bi-house-door-fill me-1"></i>Back to Home</a>
        </div>
      </div>

      <div>
        <% if (product) { %>
          <form action="/editProduct/<%= product.id %>" method="POST" enctype="multipart/form-data" class="needs-validation" novalidate>
            <div class="row g-3">
              <div class="col-12 form-group">
                <label for="name" class="form-label">Product Name</label>
                <input type="text" id="name" name="name" class="form-control form-control-lg" value = <%= product.productName %> required>
              </div>

              <div class="col-md-6 form-group">
                <label for="quantity" class="form-label">Quantity</label>
                <input type="number" id="quantity" name="quantity" class="form-control" min="0" step="1" value = <%= product.quantity %> required>
              </div>

              <div class="col-md-6 form-group">
                <label for="price" class="form-label">Price ($)</label>
                <input type="number" id="price" name="price" class="form-control" min="0" step="0.01" value = <%= product.price %> required>
              </div>

              <div class="col-12 col-md-6 form-group">
                <label for="image" class="form-label">Current Image</label>
                <div class="d-flex align-items-center gap-3">
                  <div class="image-preview">
                    <img src="/images/<%= product.image %>" alt="<%= product.productName %>">
                  </div>

                  <div class="flex-fill">
                    <input type="text" class="form-control" name="currentImage" value = <%= product.image %> readonly>
                    <div class="small text-muted mt-1">Filename shown. To replace, choose a new image below.</div>
                  </div>
                </div>
              </div>

              <div class="col-12 col-md-6 form-group">
                <label for="image" class="form-label">New Image</label>
                <input type="file" id="image" name="image" class="form-control" accept="image/*" value=<%= product.image %> >
                <div class="small text-muted mt-1">Optional. JPG or PNG. Leave empty to keep current image.</div>
              </div>

              <div class="col-12 form-group">
                <label for="category" class="form-label">Category</label>
                <select name="category" id="category" class="form-select" required>
                  <option value="Produce" <%= product.category === 'Produce' ? 'selected' : '' %>>Produce</option>
                  <option value="Drinks" <%= product.category === 'Drinks' ? 'selected' : '' %>>Drinks</option>
                  <option value="Others" <%= product.category === 'Others' ? 'selected' : '' %>>Others</option>
                </select>
              </div>

              <div class="col-12 mt-2">
                <div class="d-grid">
                  <button type="submit" class="btn btn-primary btn-update btn-lg w-100">Update Product</button>
                </div>
              </div>

              <div class="col-12 text-center mt-2 d-sm-none">
                <a href ="/" class="back-link"><i class="bi bi-arrow-left me-1"></i>Back</a>
              </div>
            </div>
          </form>
        <% } else { %>
          <div class="text-center py-4">
            <p class="mb-2">No product found.</p>
            <a href="/" class="btn btn-outline-secondary">Back</a>
          </div>
        <% } %>
      </div>
    </div>
  </main>

  <script>
    // simple client-side validation visual (visual only)
    (function () {
      'use strict'
      const forms = document.querySelectorAll('.needs-validation')
      Array.prototype.slice.call(forms).forEach(function (form) {
        form.addEventListener('submit', function (event) {
          if (!form.checkValidity()) {
            event.preventDefault()
            event.stopPropagation()
          }
          form.classList.add('was-validated')
        }, false)
      })
    })();
  </script>

<%- include("./partials/footer") %>
</body>
</html>
