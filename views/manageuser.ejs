<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <title>User Management</title>

  <style>
    :root{
      --brand:#0d6efd;
      --muted:#6c757d;
      --card-radius:12px;
    }

    body {
      background-color: lightblue;
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      color: #0b2540;
    }

    .topbar {
      background: #0b233f;
      color: #fff;
      box-shadow: 0 4px 16px rgba(11,33,64,0.12);
    }

    .topbar .navbar-brand { font-weight:700; letter-spacing:.3px; }
    .page-title { font-weight:700; font-size:1.25rem; margin-bottom:.25rem; }
    .page-sub { color:var(--muted); font-size:.95rem; }

    .card-dashboard {
      background:#fff;
      border-radius: var(--card-radius);
      box-shadow: 0 8px 24px rgba(16,24,40,0.06);
      padding:1rem;
    }

    /* Add User form */
    .add-user-card { border-radius: calc(var(--card-radius) - 2px); padding:1.1rem; }
    .add-user-header { display:flex; gap:.75rem; align-items:center; }
    .avatar-placeholder {
      width:52px; height:52px; border-radius:10px;
      background: linear-gradient(180deg,#e9f5ff,#dff0ff); display:flex; align-items:center; justify-content:center;
      color:var(--brand); font-weight:700; font-size:1.25rem;
    }

    .form-control, .form-select { border-radius:.6rem; }

    /* Users table */
    .users-table {
      width:100%;
      border-radius: var(--card-radius);
      overflow:hidden;
      box-shadow: 0 8px 24px rgba(16,24,40,0.06);
      background:#fff;
    }

    .users-table thead th {
      background: linear-gradient(180deg,#f8fbff,#eef6ff);
      border-bottom:1px solid rgba(13,110,253,0.06);
      color:#0b2540;
      font-weight:600;
    }

    .users-table tbody tr:hover { background: #fbfdff; transform: translateY(-1px); transition: .12s ease; }

    .action-btn {
      min-width:88px;
    }

    .btn-save {
      background: #fff3cd;
      color:#856404;
      border:1px solid rgba(133,100,4,0.1);
    }

    .btn-delete {
      background: #fff;
      color:#dc3545;
      border:1px solid rgba(220,53,69,0.08);
    }

    .small-muted { color:var(--muted); font-size:.92rem; }

    @media (max-width:767.98px) {
      .desktop-only { display:none !important; }
      .mobile-stacked { gap:.5rem; flex-direction:column; }
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-sm topbar">
    <div class="container-fluid">
      <a class="navbar-brand text-white" href="/">Supermarket App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-3">
          <li class="nav-item"><a class="nav-link text-white" href="/inventory">View Inventory</a></li>
          <li class="nav-item"><a class="nav-link text-white" href="/addProduct">Add Product</a></li>
          <li class="nav-item"><a class="nav-link text-white" href="/manageuser">Manage Users</a></li>
          <li class="nav-item"><a class="nav-link text-white" href="/manageOrders">Manage Orders</a></li>
          <li class="nav-item"><a class="nav-link text-white" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4 mb-5">
    <div class="d-flex justify-content-between align-items-start mb-3">
      <div>
        <div class="page-title">User Management</div>
        <div class="page-sub">Create, edit or remove users from the system</div>
      </div>

      <div class="text-end desktop-only">
        <a href="/inventory" class="btn btn-outline-light" style="background:transparent;color:#fff;border:1px solid rgba(255,255,255,0.06)">
          <i class="bi bi-box-seam me-1"></i> Inventory
        </a>
      </div>
    </div>

    <div class="row g-4">
      <!-- Add User / Form Card (left) -->
      <div class="col-lg-4">
        <div class="card-dashboard add-user-card">
          <div class="add-user-header mb-3">
            <div class="avatar-placeholder">U</div>
            <div>
              <div style="font-weight:700">Add New User</div>
              <div class="small-muted">Create a user account for the platform</div>
            </div>
          </div>

          <!-- Add User Form (keep action & inputs unchanged) -->
          <form action="/addUser" method="POST" class="row g-3">
            <div class="col-12">
              <label class="form-label small-muted">Username</label>
              <input type="text" name="username" class="form-control" placeholder="Username" required />
            </div>

            <div class="col-12">
              <label class="form-label small-muted">Email</label>
              <input type="email" name="email" class="form-control" placeholder="Email" required />
            </div>

            <div class="col-12">
              <label class="form-label small-muted">Password</label>
              <input type="password" name="password" class="form-control" placeholder="Password" required />
            </div>

            <div class="col-12">
              <label class="form-label small-muted">Address</label>
              <input type="text" name="address" class="form-control" placeholder="Address" required />
            </div>

            <div class="col-12">
              <label class="form-label small-muted">Contact</label>
              <input type="text" name="contact" class="form-control" placeholder="Contact" required />
            </div>

            <div class="col-12">
              <label class="form-label small-muted">Role</label>
              <select name="role" class="form-select" required>
                <option value="user">User</option>
                <option value="admin">Admin</option>
              </select>
            </div>

            <div class="col-12 text-end">
              <button type="submit" class="btn btn-success">
                <i class="bi bi-person-plus me-1"></i> Add User
              </button>
            </div>
          </form>
        </div>
      </div>

      <!-- Users List (right) -->
      <div class="col-lg-8">
        <div class="card-dashboard p-0">
          <div class="d-flex justify-content-between align-items-center p-3 border-bottom" style="border-color: rgba(13,110,253,0.04) !important;">
            <div>
              <strong>Existing Users</strong>
              <div class="small-muted">Manage users, promote roles or remove accounts</div>
            </div>

            <div class="small-muted">Total: <strong><%= users ? users.length : 0 %></strong></div>
          </div>

          <div class="p-3">
            <% if (users && users.length > 0) { %>
              <div class="table-responsive users-table">
                <table class="table align-middle mb-0">
                  <thead>
                    <tr>
                      <th class="text-start">ID</th>
                      <th>Username</th>
                      <th>Email</th>
                      <th>Address</th>
                      <th>Contact</th>
                      <th>Role</th>
                      <th class="text-end">Actions</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% users.forEach(user => { %>
                      <tr>
                        <!-- Edit Form (kept identical) -->
                        <form action="/promoteUser/<%= user.id %>" method="POST" class="w-100 d-flex">
                          <td style="width:6%;"><%= user.id %></td>

                          <td style="min-width:160px;">
                            <input name="username" class="form-control form-control-sm" value="<%= user.username %>" required />
                          </td>

                          <td style="min-width:200px;">
                            <input name="email" class="form-control form-control-sm" value="<%= user.email %>" required />
                          </td>

                          <td style="min-width:180px;">
                            <input name="address" class="form-control form-control-sm" value="<%= user.address %>" required />
                          </td>

                          <td style="width:110px;">
                            <input name="contact" class="form-control form-control-sm" value="<%= user.contact %>" required />
                          </td>

                          <td style="width:130px;">
                            <select name="role" class="form-select form-select-sm">
                              <option value="user" <%= user.role === 'user' ? 'selected' : '' %>>user</option>
                              <option value="admin" <%= user.role === 'admin' ? 'selected' : '' %>>admin</option>
                            </select>
                          </td>

                          <td class="text-end" style="width:210px;">
                            <button type="submit" class="btn btn-save btn-sm action-btn me-2">
                              <i class="bi bi-save2-fill me-1"></i> Save
                            </button>
                        </form>

                        <!-- Delete Button (keeps action and confirmation) -->
                        <form action="/deleteUser/<%= user.id %>" method="POST" class="d-inline" onsubmit="return confirm('Delete this user?');">
                          <button type="submit" class="btn btn-delete btn-sm action-btn">
                            <i class="bi bi-trash-fill me-1"></i> Delete
                          </button>
                        </form>
                          </td>
                      </tr>
                    <% }) %>
                  </tbody>
                </table>
              </div>
            <% } else { %>
              <div class="text-center py-4">
                <p class="mb-2 small-muted">No users found.</p>
                <a href="/addUser" class="btn btn-outline-primary"><i class="bi bi-plus-lg me-1"></i> Add your first user</a>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <%- include("./partials/footer") %>
</body>

</html>