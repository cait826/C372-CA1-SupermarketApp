<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Confirm Your Order</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body style="background-color: lightblue;">
    <div class="container mt-5">
        <h2 class="text-center mb-4">Review Your Order</h2>

        <table class="table table-bordered table-hover text-center">
            <thead class="table-dark">
                <tr>
                    <th>Product</th>
                    <th>Image</th>
                    <th>Qty</th>
                    <th>Price (each)</th>
                    <th>Subtotal</th>
                </tr>
            </thead>

            <tbody>
                <% items.forEach(item => { %>
                    <tr>
                        <td><%= item.productName %></td>
                        <td><img src="/images/<%= item.image %>" width="80"></td>
                        <td><%= item.quantity %></td>
                        <td>$<%= item.price %></td>
                        <td>$<%= item.quantity * item.price %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>

        <h3 class="text-end">Total: $<%= total %></h3>

        <p class="text-center mt-4">Is this your final order?</p>

        <form action="/cart/confirm" method="POST" class="text-center">
            <button class="btn btn-success me-2" type="submit" name="confirm" value="yes">Yes, Confirm Order</button>
            <button class="btn btn-danger" type="submit" name="confirm" value="no">No, Go Back</button>
        </form>
    </div>
</body>
</html>
