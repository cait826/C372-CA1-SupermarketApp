<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Product — Supermarket App</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    :root{
      --accent: #0d6efd;
      --muted: #6c757d;
      --card-radius: 14px;
    }

    html, body { height: 100%; }
    body {
      margin: 0;
      background: linear-gradient(180deg, rgba(224,242,255,0.6), rgba(218,237,255,0.45));
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      color: #0b2540;
    }

    /* Keep navbar structure unchanged; slight visual spacing tweak */
    .navbar { padding: .6rem 1rem; }
    .navbar .navbar-brand { background: #fff; padding: .35rem .75rem; border-radius: 8px; font-weight:700; display:flex; gap:.5rem; align-items:center; }
    .nav-link { color: rgba(74,158,255,0.95) !important; }
    .nav-link:hover { color:#fff !important; text-decoration:underline; }

    /* Main centered card */
    .main-container { max-width: 960px; margin: 2.25rem auto; padding: 0 1rem; }
    .card-panel {
      background: linear-gradient(180deg,#ffffff,#fbfdff);
      border-radius: var(--card-radius);
      padding: 1.25rem;
      border: 1px solid rgba(13,110,253,0.04);
      box-shadow: 0 14px 40px rgba(16,24,40,0.07);
    }

    .card-head {
      display:flex;
      justify-content:space-between;
      align-items:center;
      gap:1rem;
      margin-bottom:1rem;
    }
    .page-title { font-weight:800; font-size:1.25rem; margin:0; }
    .page-sub { color:var(--muted); font-size:.95rem; margin:0; }

    /* Two-column form layout */
    .form-left { padding-right: .75rem; }
    .form-right { padding-left: .75rem; }

    .form-group { margin-bottom: .85rem; }
    label.form-label { font-weight:600; color:#243447; margin-bottom:.35rem; display:block; }

    /* Image preview card */
    .image-card {
      background: #f8fbff;
      border-radius: 12px;
      padding: .85rem;
      border:1px solid rgba(13,110,253,0.04);
      box-shadow: 0 8px 20px rgba(16,24,40,0.04);
      text-align:center;
    }
    .image-preview {
      width:100%;
      max-width: 320px;
      height: 220px;
      margin: 0 auto .6rem;
      border-radius:10px;
      overflow:hidden;
      background: linear-gradient(180deg,#eef9ff,#fff);
      display:flex; align-items:center; justify-content:center;
      border:1px dashed rgba(13,110,253,0.06);
    }
    .image-preview img { width:100%; height:100%; object-fit:cover; display:block; }

    .image-caption { color:var(--muted); font-size:.9rem; margin-bottom:.5rem; }

    /* Buttons */
    .btn-update { border-radius:10px; padding:.6rem 1rem; font-weight:700; }
    .btn-back { border-radius:10px; }

    /* Responsive tweaks */
    @media(max-width:767.98px) {
      .form-left, .form-right { padding:0; }
      .image-preview { height:160px; }
      .card-panel { padding:1rem; }
    }
  </style>
</head>
<body>
  <!-- Navbar (structure unchanged) -->
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Supermarket App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/inventory" style="color: #4a9eff;">Inventory</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/addProduct" style="color: #4a9eff;">Add New Product</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/manageuser" style="color: #4a9eff;">Manage Users</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/manageOrder" style="color: #4a9eff;">Manage Orders</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/manageReviews" style="color: #4a9eff;">Manage Reviews</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout" style="color: #4a9eff;">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Main content -->
  <div class="main-container">
    <div class="card-panel">
      <div class="card-head">
        <div>
          <h1 class="page-title">Update Product</h1>
          <p class="page-sub">Edit product details and optionally replace the product image.</p>
        </div>
        <div class="d-none d-md-block text-end">
          <a href="/inventory" class="btn btn-outline-secondary btn-back"><i class="bi bi-arrow-left me-1"></i>Back to Inventory</a>
        </div>
      </div>

      <% if (product) { %>
        <form action="/updateProduct/<%= product.id %>" method="POST" enctype="multipart/form-data" class="row g-3">
          <!-- Left: inputs -->
          <div class="col-12 col-lg-7 form-left">
            <div class="form-group">
              <label for="name" class="form-label">Product Name</label>
              <input type="text" class="form-control form-control-lg" id="name" name="name" value="<%= product.productName %>" required>
            </div>

            <div class="row gx-3">
              <div class="col-6 form-group">
                <label for="quantity" class="form-label">Quantity</label>
                <input type="number" class="form-control" id="quantity" name="quantity" min="0" step="1" value="<%= product.quantity %>" required>
              </div>

              <div class="col-6 form-group">
                <label for="price" class="form-label">Price ($)</label>
                <input type="number" class="form-control" id="price" name="price" min="0" step="0.01" value="<%= product.price %>" required>
              </div>
            </div>

            <div class="form-group">
              <label class="form-label">Current Image Filename</label>
              <input type="text" class="form-control" name="currentImage" value="<%= product.image %>" readonly>
              <div class="small text-muted mt-1">Filename is shown. To replace the image, use the upload field.</div>
            </div>

            <div class="form-group">
              <label for="image" class="form-label">Upload New Image</label>
              <input class="form-control" type="file" id="image" name="image" accept="image/*">
              <div class="small text-muted mt-1">Optional. JPG or PNG. Leave empty to keep current image.</div>
            </div>

            <div class="form-group">
              <label for="category" class="form-label">Category</label>
              <select name="category" id="category" class="form-select" required>
                <option value="Produce" <%= product.category === 'Produce' ? 'selected' : '' %>>Produce</option>
                <option value="Drinks" <%= product.category === 'Drinks' ? 'selected' : '' %>>Drinks</option>
                <option value="Others" <%= product.category === 'Others' ? 'selected' : '' %>>Others</option>
              </select>
            </div>

            <div class="d-grid gap-2 d-lg-flex justify-content-lg-end mt-3">
              <button type="submit" class="btn btn-primary btn-update w-100 w-lg-auto">Update Product</button>
              <a href="/inventory" class="btn btn-outline-secondary btn-back w-100 w-lg-auto">Back to Inventory</a>
            </div>
          </div>

          <!-- Right: image preview -->
          <div class="col-12 col-lg-5 form-right">
            <div class="image-card">
              <div class="image-caption">Current Product Image</div>
              <div class="image-preview" aria-hidden="true">
                <img src="/images/<%= product.image %>" alt="<%= product.productName %>">
              </div>

              <div class="small text-muted">Product: <strong><%= product.productName %></strong></div>
              <div class="small text-muted mt-2">Category: <strong><%= product.category %></strong></div>

              <div class="mt-3">
                <div class="small text-muted">Preview box is for reference. Uploading a new image will replace the current one after update.</div>
              </div>
            </div>
          </div>
        </form>
      <% } else { %>
        <div class="text-center py-4">
          <p class="mb-3">No product found.</p>
          <a href="/inventory" class="btn btn-outline-secondary">Back to Inventory</a>
        </div>
      <% } %>
    </div>
  </div>

<%- include("./partials/footer") %>
</body>
</html>
