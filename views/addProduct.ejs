<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    :root {
      --accent: #0d6efd;
      --muted: #6c757d;
      --card-radius: 14px;
    }

    html, body { height: 100%; }
    body {
      margin: 0;
      background: linear-gradient(180deg, rgba(224,242,255,0.55), rgba(218,237,255,0.4));
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      color: #0b2540;
    }

    /* Navbar */
    .admin-nav {
      background: linear-gradient(90deg, rgba(11,35,64,0.98), #0b233f);
      box-shadow: 0 6px 20px rgba(11,35,64,0.12);
    }
    .admin-nav .navbar-brand {
      background: #fff;
      color: #0b2540;
      padding: .35rem .75rem;
      border-radius: 8px;
      font-weight:700;
      display:flex;
      align-items:center;
      gap:.6rem;
    }
    .admin-nav .nav-link { color: rgba(74,158,255,0.95) !important; }
    .admin-nav .nav-link.active { color: #fff !important; font-weight:600; }

    /* Page container */
    .page-wrap { max-width: 920px; margin: 2.25rem auto; padding: 0 1rem; }

    /* Header */
    .page-header {
      display:flex;
      justify-content:space-between;
      align-items:center;
      gap:1rem;
      margin-bottom:1rem;
    }
    .page-title { font-size:1.4rem; font-weight:800; }
    .page-sub { color: var(--muted); }

    /* Card form */
    .card-panel {
      background: linear-gradient(180deg,#ffffff,#fbfdff);
      border-radius: var(--card-radius);
      padding: 1.25rem;
      box-shadow: 0 12px 36px rgba(16,24,40,0.08);
      border: 1px solid rgba(13,110,253,0.04);
    }

    .form-section { gap:1rem; display:grid; grid-template-columns: 1fr; }
    @media(min-width:768px) {
      .form-section { grid-template-columns: 1fr 1fr; }
    }

    .form-label { font-weight:600; margin-bottom:.35rem; color:#243447; display:block; }
    .input-help { color:var(--muted); font-size:.9rem; margin-top:.35rem; }

    .image-preview {
      width:100%;
      height:140px;
      border-radius:8px;
      background:#f6fbff;
      display:flex;
      align-items:center;
      justify-content:center;
      color:var(--muted);
      border:1px dashed rgba(13,110,253,0.06);
      overflow:hidden;
      object-fit:cover;
    }

    .btn-submit {
      border-radius:10px;
      padding: .65rem 1rem;
      font-weight:700;
    }

    .meta-row { display:flex; gap:.5rem; align-items:center; flex-wrap:wrap; margin-top:.75rem; color:var(--muted); }

    /* Footer spacing */
    .footer-gap { height:48px; }
  </style>

  <title>Supermarket App — Add Product</title>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-sm admin-nav">
    <div class="container page-wrap">
      <a class="navbar-brand" href="/">
        <span style="width:34px;height:34px;border-radius:6px;display:inline-flex;align-items:center;justify-content:center;background:linear-gradient(180deg,#e9f5ff,#dff0ff);color:var(--accent);font-weight:800">🛒</span>
        <span>Supermarket App</span>
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/inventory" style="color: #4a9eff;">Inventory</a></li>
          <li class="nav-item"><a class="nav-link active" href="/addProduct" style="color: #fff;">Add New Product</a></li>
          <li class="nav-item"><a class="nav-link" href="/manageuser" style="color: #4a9eff;">Manage Users</a></li>
          <li class="nav-item"><a class="nav-link" href="/manageOrder" style="color: #4a9eff;">Manage Orders</a></li>
          <li class="nav-item"><a class="nav-link" href="/manageReviews" style="color: #4a9eff;">Manage Reviews</a></li>
          <li class="nav-item"><a class="nav-link" href="/logout" style="color: #4a9eff;">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Main content -->
  <div class="page-wrap">
    <div class="page-header">
      <div>
        <div class="page-title">Add New Product</div>
        <div class="page-sub">Fill in the product details below to create a listing.</div>
      </div>
      <div class="meta-row">
        <div class="small"><i class="bi bi-clock-history me-1"></i> Quick add</div>
        <div class="small"><i class="bi bi-shield-lock me-1"></i> Secure</div>
      </div>
    </div>

    <div class="card-panel">
      <!-- Keep action, method, enctype and field names/IDs unchanged -->
      <form action="/addProduct" method="POST" enctype="multipart/form-data" class="row g-3">
        <div class="col-12">
          <label for="name" class="form-label">Product Name</label>
          <input type="text" class="form-control form-control-lg" id="name" name="name" required>
        </div>

        <div class="col-12 form-section">
          <div>
            <label for="quantity" class="form-label">Quantity</label>
            <input type="number" class="form-control" id="quantity" name="quantity" min="0" step="1" required>
          </div>

          <div>
            <label for="price" class="form-label">Price ($)</label>
            <input type="number" class="form-control" id="price" name="price" min="0" step="0.01" required>
          </div>
        </div>

        <div class="col-12 col-md-6">
          <label for="category" class="form-label">Category</label>
          <select name="category" id="category" class="form-select" required>
              <option value="">Select Category</option>
              <option value="Produce">Produce</option>
              <option value="Drinks">Drinks</option>
              <option value="Others">Others</option>
          </select>
        </div>

        <div class="col-12 col-md-6">
          <label for="image" class="form-label">Product Image</label>
          <input class="form-control" type="file" id="image" name="image" accept="image/*" required>
          <div class="input-help">Recommended size: 800x600px. JPG or PNG.</div>
        </div>

        <div class="col-12">
          <div class="image-preview" aria-hidden="true">
            <i class="bi bi-image" style="font-size:1.6rem;"></i>
            <span class="ms-2 small" style="color:var(--muted)">Image preview will appear here after upload (UI only)</span>
          </div>
        </div>

        <div class="col-12">
          <div class="d-grid">
            <button type="submit" class="btn btn-primary btn-submit btn-lg w-100">Add Product</button>
          </div>
        </div>
      </form>
    </div>

    <div class="footer-gap"></div>
  </div>

  <%- include("./partials/footer") %>
</body>
</html>
